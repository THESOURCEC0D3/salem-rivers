# Page Overrides — Give (`/give`)

> **Status: deliberate placeholder.** A polished "Coming soon" notice (mirrors `/watch`), so the hero's
> "Give online" button lands somewhere intentional instead of a 404.

## Planned (later, not yet built)

Secure online giving (architecture doc §5 — CONDITIONAL, understated, never a co-equal hero CTA):
- A `/give` route with **Paystack or Flutterwave** integration.
- Flip `church.give.enabled` to `true` to surface the footer "Give" link once giving is live.
- Keep it understated — giving supports the church; it never competes with Plan Your Visit.
